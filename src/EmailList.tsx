import React from 'react';

interface IProps {
    // name: string
}

const EmailList = (props: IProps) => {
    return (
        <div>
            Email List
        </div>
    )
}


export default EmailList

