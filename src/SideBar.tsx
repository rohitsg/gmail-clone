import React from 'react';

interface IProps {
    // name: string
}

const SideBar = (props: IProps) => {
    return (
        <div>
            Side bar1
        </div>
    )
}


export default SideBar

